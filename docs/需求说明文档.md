# 国密加密服务平台软件需求说明书

| 项目     | 内容                                             |
| -------- | ------------------------------------------------ |
| 文档名称 | 国密加密服务平台软件需求说明书                   |
| 文档版本 | V1.1                                             |
| 编写日期 | 2026-09-17                                       |
| 文档状态 | 修订版                                           |
| 上一版本 | V1.0                                             |
| 文档用途 | 需求分析、系统设计、开发实施、测试验收及项目评审 |

---

# 目录

1. 项目概述
   1.1 建设背景
   1.2 建设目标
   1.3 建设范围
   1.4 术语与缩略语
   1.5 读者对象
   1.6 建设原则
2. 系统总体架构
   2.1 架构概览
   2.2 系统分层
   2.3 模块划分
   2.4 安全边界
   2.5 密码服务处理模型
   2.6 密码设备适配模型
3. 功能需求
   3.1 密码算法服务
   3.2 密钥生命周期管理
   3.3 应用认证与访问控制
   3.4 审计与安全管理
   3.5 风险控制与安全事件
   3.6 管理控制台
   3.7 密码设备管理
   3.8 系统配置管理
   3.9 健康检查与运行状态
4. 非功能需求
   4.1 性能需求
   4.2 可用性需求
   4.3 可扩展性需求
   4.4 可维护性需求
   4.5 兼容性需求
   4.6 安全性需求
   4.7 容量需求
   4.8 时间同步需求
5. 运行环境
   5.1 硬件环境
   5.2 操作系统
   5.3 数据库
   5.4 缓存及中间件
   5.5 密码设备
   5.6 网络环境
   5.7 国产化适配要求
6. 接口规范
   6.1 通用约定
   6.2 认证要求
   6.3 请求与响应格式
   6.4 密码服务接口
   6.5 密钥管理接口
   6.6 应用认证接口
   6.7 审计接口
   6.8 系统及健康检查接口
   6.9 幂等与防重放
   6.10 错误码
   6.11 接口版本管理
7. 数据需求
   7.1 核心数据实体
   7.2 密钥数据
   7.3 应用及凭据数据
   7.4 审计数据
   7.5 安全事件数据
   7.6 数据完整性
   7.7 数据保留
   7.8 数据备份
   7.9 数据恢复验证
   7.10 数据库权限
8. 安全需求
   8.1 密钥安全
   8.2 传输安全
   8.3 身份认证安全
   8.4 权限控制
   8.5 管理员安全
   8.6 Token 安全
   8.7 API 安全
   8.8 审计安全
   8.9 日志安全
   8.10 密码设备安全
   8.11 安全事件处置
9. 需求修正与补充说明
   9.1 V1.0 逻辑修正
   9.2 V1.1 新增需求
   9.3 V1.1 明确事项
   9.4 V1.1 范围边界
10. 测试与验收需求
    10.1 需求追踪
    10.2 功能测试
    10.3 算法测试
    10.4 密钥生命周期测试
    10.5 安全测试
    10.6 性能测试
    10.7 高可用测试
    10.8 备份恢复测试
    10.9 国产化兼容性测试
11. 需求编号与追踪矩阵

---

# 1. 项目概述

## 1.1 建设背景

随着业务系统国产密码算法应用和密码安全建设要求不断提高，传统由各业务系统自行实现加密、解密、签名、验签以及密钥管理的方式存在以下问题：

1. 各业务系统密码算法实现不统一，导致互操作性差；
2. 密钥分散存储，存在明文保存、泄露及权限失控风险；
3. 密钥生命周期管理能力不足；
4. 缺乏统一的密码服务接口；
5. 不同系统重复建设密码服务；
6. 加解密操作缺乏统一审计；
7. 密钥使用过程难以追溯；
8. 业务系统直接接触密钥材料，增加密钥泄露风险；
9. 密码设备及软件密码模块缺乏统一管理；
10. 缺少统一的密钥备份、恢复、轮换和销毁机制；
11. 难以形成统一的密码服务安全边界。

因此，建设统一的国密加密服务平台，对 SM2、SM3、SM4、HMAC-SM3 等密码算法提供统一服务，同时实现密钥全生命周期管理、统一身份认证、权限控制、审计、安全事件管理及密码设备管理。

---

## 1.2 建设目标

平台总体目标是建立统一、安全、标准化、可审计的国密密码服务能力，为业务系统提供统一密码基础设施。

### 1.2.1 统一密码算法

平台统一提供：

* SM2 非对称密码服务；
* SM3 杂凑服务；
* SM4 对称密码服务；
* HMAC-SM3 消息认证服务；
* 安全随机数服务。

### 1.2.2 统一密钥管理

建立密钥全生命周期管理机制，实现：

> 生成 → 创建 → 激活 → 使用 → 轮换 → 禁用 → 过期/注销 → 销毁

并支持密钥版本管理、密钥备份、恢复及恢复验证。

### 1.2.3 统一身份认证

业务系统通过 AppID + AppSecret 获取访问凭证，平台对调用方进行身份认证及权限检查。

对于高安全等级场景，应支持进一步增加请求签名、双向 TLS、IP 白名单等安全控制。

### 1.2.4 统一审计

完整记录：

> 谁 → 什么时间 → 来自哪里 → 使用什么资源 → 执行什么操作 → 操作结果 → 请求耗时

并建立不可随意修改、删除的审计机制。

### 1.2.5 密钥不出安全边界

平台托管的 SM2 私钥、SM4 密钥、HMAC 密钥等敏感密钥材料不得通过普通 API 返回给业务系统。

业务系统原则上仅持有：

* AppID；
* AppSecret 或其他认证凭据；
* KeyId；
* 必要的业务密文、签名及摘要数据。

---

## 1.3 建设范围

### 1.3.1 本期建设范围

1. SM2；
2. SM3；
3. SM4；
4. HMAC-SM3；
5. 随机数；
6. 密钥生成；
7. 密钥激活、禁用、重新激活；
8. 密钥轮换；
9. 密钥过期；
10. 密钥注销；
11. 密钥销毁；
12. 密钥版本管理；
13. 密钥导入；
14. 密钥备份；
15. 密钥恢复；
16. 密钥恢复验证；
17. AppID/AppSecret；
18. Access Token；
19. API 权限；
20. 应用级限流和配额；
21. 管理控制台；
22. 审计日志；
23. 日志完整性保护；
24. 风险检测；
25. 安全事件及告警；
26. 密码设备管理；
27. 系统配置；
28. 健康检查；
29. 高可用及故障恢复。

### 1.3.2 本期不包含

以下内容原则上不纳入本期平台业务授权体系：

* 业务系统自身的角色权限；
* 业务系统数据权限；
* 业务系统业务流程权限；
* 业务系统业务数据加密策略以外的业务规则。

---

## 1.4 术语与缩略语

| 术语         | 说明                                                    |
| ------------ | ------------------------------------------------------- |
| SM2          | 国密非对称密码算法                                      |
| SM3          | 国密密码杂凑算法                                        |
| SM4          | 国密对称分组密码算法                                    |
| HMAC-SM3     | 基于 SM3 的 HMAC 消息认证码                             |
| KMS          | Key Management Service，密钥管理服务                    |
| HSM          | Hardware Security Module，硬件安全模块                  |
| KeyId        | 平台内部密钥唯一标识                                    |
| KeyVersion   | 密钥版本                                                |
| AppID        | 应用唯一标识                                            |
| AppSecret    | 应用认证凭据                                            |
| Access Token | API 访问令牌                                            |
| Root Key     | 用于保护平台密钥材料的根密钥                            |
| CSPRNG       | Cryptographically Secure Pseudo-Random Number Generator |
| Nonce        | 一次性随机数或唯一值                                    |
| AAD          | Additional Authenticated Data                           |
| RequestId    | 单次 API 请求唯一标识                                   |
| TraceId      | 分布式请求追踪标识                                      |
| SIEM         | Security Information and Event Management 的缩写。安全信息和事件管理系统|

---

## 1.5 读者对象

本文档适用于：

* 项目管理人员；
* 系统架构师；
* 开发人员；
* 测试人员；
* 安全人员；
* 运维人员；
* 审计人员；
* 业务系统对接人员；
* 密码设备厂商及实施人员。

---

## 1.6 建设原则

### 1.6.1 密钥不出安全边界

敏感密钥材料原则上只允许在密码服务安全边界内部使用。

### 1.6.2 最小权限

应用、用户、服务账号及数据库账号仅授予完成职责所需的最小权限。

### 1.6.3 默认安全

默认关闭高风险算法及高风险配置；如因兼容性必须开放，应进行显式配置。

### 1.6.4 全程审计

密钥管理、密码运算、认证、授权、配置变更、安全事件等关键操作均必须产生审计记录。

### 1.6.5 可替换密码设备

密码服务核心逻辑与具体 HSM 厂商实现解耦，通过统一密码服务提供者接口进行适配。

---

# 2. 系统总体架构

## 2.1 架构概览

平台总体架构如下：

```text
┌──────────────────────────────────────────────┐
│                 业务应用系统层               │
│  业务系统A      业务系统B      业务系统C     │
└──────────────────────┬───────────────────────┘
                       │ HTTPS / REST API
                       ▼
┌──────────────────────────────────────────────┐
│                  API 接入层                   │
│  API Gateway / Authentication / RateLimit   │
└──────────────────────┬───────────────────────┘
                       │
       ┌───────────────┴────────────────┐
       ▼                                ▼
┌──────────────────┐          ┌──────────────────┐
│ 身份认证与权限模块 │          │ 管理控制台         │
└────────┬─────────┘          └────────┬─────────┘
         │                             │
         └──────────────┬──────────────┘
                        ▼
┌──────────────────────────────────────────────┐
│                 核心密码服务层                │
│ SM2 │ SM3 │ SM4 │ HMAC-SM3 │ RNG             │
└──────────────────────┬───────────────────────┘
                       ▼
┌──────────────────────────────────────────────┐
│                  密钥管理层                   │
│ Key Lifecycle │ Version │ Import │ Backup   │
└──────────────────────┬───────────────────────┘
                       ▼
┌──────────────────────────────────────────────┐
│                 Crypto Provider              │
│ Software Provider / HSM Provider             │
└──────────────┬───────────────────┬───────────┘
               ▼                   ▼
        ┌─────────────┐      ┌─────────────┐
        │ 软件密码模块 │      │ HSM密码设备  │
        └─────────────┘      └─────────────┘

┌──────────────────────────────────────────────┐
│             审计 / 风险 / 安全事件层          │
└──────────────────────────────────────────────┘
```

---

## 2.2 系统分层

平台划分为：

| 层次           | 主要职责                             |
| -------------- | ------------------------------------ |
| API 接入层     | API 路由、认证、限流、参数校验       |
| 身份认证层     | AppID/AppSecret、Token、请求签名     |
| 权限控制层     | API、KeyUsage、App 数据隔离          |
| 密码服务层     | SM2、SM3、SM4、HMAC、RNG             |
| 密钥管理层     | 密钥生命周期、版本、导入、备份、恢复 |
| 密码设备适配层 | HSM、软件密码模块统一适配            |
| 数据层         | 业务数据、密钥元数据、审计数据       |
| 安全管理层     | 风险检测、告警、安全事件             |
| 运维管理层     | 配置、监控、健康检查                 |

---

## 2.3 模块划分

| 模块             | 职责                        |
| ---------------- | --------------------------- |
| API 接口模块     | 提供统一 REST API           |
| 身份认证模块     | 应用身份验证及 Token 管理   |
| 权限控制模块     | API、KeyUsage、资源权限控制 |
| 密码服务模块     | 执行 SM2/SM3/SM4/HMAC/RNG   |
| 密钥管理模块     | 管理密钥生命周期            |
| 密钥版本模块     | 管理历史密钥版本            |
| 密钥备份恢复模块 | 负责备份、恢复及验证        |
| 密码设备模块     | HSM/软件密码模块管理        |
| 审计模块         | 全量审计                    |
| 风险控制模块     | 异常调用检测                |
| 安全事件模块     | 告警、处置、关闭            |
| 管理控制台       | 平台管理 UI                 |
| 配置管理模块     | 平台安全策略配置            |
| 监控模块         | 服务状态及运行指标          |

---

## 2.4 安全边界

平台必须明确密码安全边界。

业务系统不得直接访问：

* SM2 私钥；
* SM4 密钥；
* HMAC 密钥；
* Root Key；
* HSM 内部密钥；
* 密钥加密密钥。

业务系统通过 KeyId 请求密码操作。

---

## 2.5 密码服务处理模型

标准密码操作流程：

```text
请求
 ↓
HTTPS
 ↓
身份认证
 ↓
权限检查
 ↓
KeyId校验
 ↓
KeyUsage校验
 ↓
Key状态检查
 ↓
密码服务
 ↓
Crypto Provider
 ↓
HSM/软件密码模块
 ↓
返回密文/签名/摘要
 ↓
写入审计
```

---

## 2.6 密码设备适配模型

平台应定义统一 Crypto Provider 接口。

至少包括：

```text
GenerateKey()
Encrypt()
Decrypt()
Sign()
Verify()
Hash()
Mac()
GenerateRandom()
DestroyKey()
```

实现方式包括：

* SoftwareCryptoProvider；
* HsmCryptoProvider。

生产环境实际采用哪一种方式，应根据部署环境、安全等级、密码设备配置及项目验收要求最终确定。

---

# 3. 功能需求

# 3.1 密码算法服务

## 3.1.1 SM2

| 编号      | 功能         | 要求                             |
| --------- | ------------ | -------------------------------- |
| F-SM2-001 | 密钥对生成   | 生成 SM2 公私钥对                |
| F-SM2-002 | 公钥加密     | 使用指定 KeyId 对数据加密        |
| F-SM2-003 | 私钥解密     | 使用指定 KeyId 解密              |
| F-SM2-004 | 私钥签名     | 对数据进行签名                   |
| F-SM2-005 | 公钥验签     | 验证签名                         |
| F-SM2-006 | 签名编码转换 | 根据约定支持指定签名编码         |
| F-SM2-007 | 密文编码处理 | 根据实际密码模块能力支持约定编码 |

### 算法参数要求

* 使用符合项目要求的 SM2 曲线参数；
* 签名格式应明确 DER 或 Raw；
* 密文格式应明确实际密码设备/密码库支持的编码；
* 不同编码之间不得由调用方自行猜测；
* 接口必须显式声明编码格式或使用平台统一默认值；
* SM2 单次数据长度限制必须明确并可配置；
* 超过限制时返回明确错误码。

> SM2 密文的 DER、C1C3C2 等编码方式必须以实际采用的密码设备、密码库及项目互操作要求为准，详细设计阶段进行最终确认，不在需求阶段无依据地固定某一种实现。

---

# 3.1.2 SM3

| 编号      | 功能      | 要求                     |
| --------- | --------- | ------------------------ |
| F-SM3-001 | SM3 Hash  | 对数据计算 SM3           |
| F-SM3-002 | 流式 Hash | 支持大数据流             |
| F-SM3-003 | HMAC-SM3  | 使用 HMAC-SM3 计算认证码 |
| F-SM3-004 | HMAC 验证 | 验证 HMAC-SM3            |

要求：

* 摘要长度为 256 位；
* 支持 Base64；
* 支持 Hex；
* 支持字节流；
* 大文件不得强制一次性加载到内存。

---

# 3.1.3 SM4

| 编号      | 功能         | 要求                 |
| --------- | ------------ | -------------------- |
| F-SM4-001 | SM4 密钥生成 | 生成 128 位 SM4 密钥 |
| F-SM4-002 | SM4 加密     | 指定 KeyId 加密      |
| F-SM4-003 | SM4 解密     | 指定 KeyId 解密      |
| F-SM4-004 | 流式加密     | 支持大数据           |
| F-SM4-005 | 流式解密     | 支持大数据           |

支持模式：

* ECB；
* CBC；
* CTR；
* GCM。

其中：

* ECB 仅用于兼容已有业务；
* 新业务原则上不得默认使用 ECB；
* CBC 必须明确 IV 和 Padding；
* CTR 必须明确计数器/Nonce 规则；
* GCM 必须明确 Nonce、Tag、AAD；
* GCM Nonce 不得在同一密钥下重复使用；
* GCM Tag 验证失败时必须整体判定解密失败，不得输出未经验证的数据。

具体 IV、Nonce、Tag 长度以及密码设备支持范围在详细设计阶段确定。

---

# 3.1.4 HMAC-SM3

| 编号       | 功能          | 要求       |
| ---------- | ------------- | ---------- |
| F-HMAC-001 | HMAC-SM3 生成 | 生成认证码 |
| F-HMAC-002 | HMAC-SM3 验证 | 验证认证码 |

要求：

* HMAC 密钥由平台统一管理；
* 不允许调用方提交平台托管密钥明文；
* 输出支持 Base64/Hex；
* 验证失败只返回验证失败结果，不泄露内部比较过程。

---

# 3.1.5 随机数服务

| 编号      | 功能       | 要求                 |
| --------- | ---------- | -------------------- |
| F-RNG-001 | 随机数生成 | 生成密码学安全随机数 |
| F-RNG-002 | 指定长度   | 支持指定字节长度     |
| F-RNG-003 | 输出编码   | 支持 Base64/Hex      |

随机数必须使用密码学安全随机数生成机制。

---

# 3.2 密钥生命周期管理

## 3.2.1 密钥分类

| KeyType | 算法     | 用途           |
| ------- | -------- | -------------- |
| SM2     | SM2      | SIGN / ENCRYPT |
| SM4     | SM4      | ENCRYPT        |
| HMAC    | HMAC-SM3 | MAC            |

AppSecret 不作为普通密码学 Key 纳入密钥材料管理，而作为应用认证凭据独立管理。

---

## 3.2.2 密钥基本属性

每个密钥至少包括：

* KeyId；
* KeyType；
* KeyUsage；
* KeyVersion；
* AppId；
* 状态；
* 创建时间；
* 生效时间；
* 过期时间；
* 轮换策略；
* 创建者；
* 创建来源；
* 密码设备；
* 算法参数；
* 销毁时间；
* 元数据完整性信息。

KeyType、KeyUsage、所属 App 等核心属性创建后原则上不可直接修改。

如需改变，应创建新的密钥。

---

## 3.2.3 生命周期状态

```text
CREATED
   │
   ▼
ACTIVE
   │
   ├──────────────► DISABLED
   │                    │
   │                    └────► ACTIVE
   │
   ├──────────────► ROTATED
   │
   ├──────────────► EXPIRED
   │
   ├──────────────► REVOKED
   │
   └──────────────► DESTROYED
```

“使用”不是独立状态，而是 ACTIVE 状态下执行的密码操作。

---

## 3.2.4 状态转换矩阵

| 当前状态  | 目标状态  | 是否允许 | 条件                         |
| --------- | --------- | -------: | ---------------------------- |
| CREATED   | ACTIVE    |       是 | 激活                         |
| CREATED   | REVOKED   |       是 | 异常/人工撤销                |
| CREATED   | DESTROYED |       是 | 销毁                         |
| ACTIVE    | DISABLED  |       是 | 临时禁用                     |
| ACTIVE    | ROTATED   |       是 | 新版本产生                   |
| ACTIVE    | EXPIRED   | 系统自动 | 到期                         |
| ACTIVE    | REVOKED   |       是 | 安全事件                     |
| ACTIVE    | DESTROYED |       是 | 满足销毁条件                 |
| DISABLED  | ACTIVE    |       是 | 重新激活                     |
| DISABLED  | REVOKED   |       是 | 永久撤销                     |
| DISABLED  | DESTROYED |       是 | 销毁                         |
| ROTATED   | ACTIVE    |       否 | 不允许旧版本重新成为当前版本 |
| ROTATED   | DESTROYED |       是 | 满足销毁条件                 |
| EXPIRED   | ACTIVE    |       否 | 原则上不得重新激活           |
| EXPIRED   | DESTROYED |       是 | 销毁                         |
| REVOKED   | ACTIVE    |       否 | 不允许重新激活               |
| REVOKED   | DESTROYED |       是 | 销毁                         |
| DESTROYED | 任意      |       否 | 终态                         |

---

## 3.2.5 密钥生成

| 编号     | 功能     |
| -------- | -------- |
| F-KM-001 | 密钥生成 |

要求：

* 根据 KeyType 生成密钥；
* 自动生成唯一 KeyId；
* 不返回对称密钥明文；
* SM2 可返回公钥；
* 返回密钥元数据；
* 生成操作必须产生审计记录。

---

## 3.2.6 密钥查询

| 编号     | 功能     |
| -------- | -------- |
| F-KM-002 | 密钥查询 |
| F-KM-010 | 密钥列表 |

不得返回：

* SM2 私钥；
* SM4 密钥；
* HMAC 密钥；
* Root Key；
* KEK；
* AppSecret 明文。

---

## 3.2.7 密钥激活

| 编号     | 功能     |
| -------- | -------- |
| F-KM-003 | 密钥激活 |

只有符合状态转换规则的 CREATED 密钥可以激活。

---

## 3.2.8 密钥禁用

| 编号     | 功能     |
| -------- | -------- |
| F-KM-004 | 密钥禁用 |

禁用后：

* 加密禁止；
* 解密禁止；
* 签名禁止；
* 验签原则上禁止；
* 查询允许；
* 管理操作必须记录审计。

如存在历史数据恢复场景，需要在详细设计中明确是否允许特定历史解密。

---

## 3.2.9 密钥重新激活

| 编号     | 功能         |
| -------- | ------------ |
| F-KM-005 | 密钥重新激活 |

原则上仅 DISABLED 状态允许重新激活。

---

## 3.2.10 密钥轮换

| 编号     | 功能     |
| -------- | -------- |
| F-KM-006 | 密钥轮换 |

轮换后：

```text
Key-001 Version-1 → ROTATED
Key-001 Version-2 → ACTIVE
```

要求：

* KeyId 可以保持不变；
* KeyVersion 递增；
* 新版本产生新的密钥材料；
* 旧版本保留必要历史能力；
* 新产生的数据默认使用当前 ACTIVE 版本；
* 历史数据可以根据版本信息选择历史版本处理。

---

## 3.2.11 密钥轮换策略

平台应支持：

1. 按时间轮换；
2. 按调用次数轮换；
3. 按数据量轮换；
4. 手工轮换；
5. 安全事件触发轮换。

轮换策略至少包括：

* 轮换周期；
* 最大调用次数；
* 最大数据量；
* 提前轮换时间；
* 是否自动轮换；
* 轮换失败处理策略。

---

## 3.2.12 密钥过期

| 编号     | 功能     |
| -------- | -------- |
| F-KM-007 | 密钥过期 |

系统应根据 ExpireAt 自动检查并变更状态。

时间判断必须依赖统一时间源。

---

## 3.2.13 密钥注销

| 编号     | 功能     |
| -------- | -------- |
| F-KM-008 | 密钥注销 |

REVOKED 后：

* 禁止新的加密；
* 禁止新的签名；
* 禁止新的密钥使用；
* 历史解密/验签是否允许必须根据安全策略确定。

---

## 3.2.14 密钥销毁

| 编号     | 功能     |
| -------- | -------- |
| F-KM-009 | 密钥销毁 |

销毁必须：

1. 校验当前状态；
2. 校验操作权限；
3. 二次确认；
4. 记录审计；
5. 执行密钥材料安全擦除；
6. 验证材料不可恢复；
7. 保留必要元数据。

DESTROYED 为终态。

---

## 3.2.15 密钥导入

新增：

| 编号     | 功能     |
| -------- | -------- |
| F-KM-012 | 密钥导入 |

支持根据实际业务需求导入：

* SM2 公钥；
* SM2 密钥对；
* SM4 密钥；
* HMAC 密钥。

要求：

* 导入过程必须加密保护；
* 不允许通过普通 HTTP 明文导入；
* 导入后的密钥不得在普通 API 响应中返回；
* 必须验证 KeyType 与 KeyUsage；
* 导入操作必须审计。

对于私钥及对称密钥导入，优先采用密码设备支持的安全导入机制。

---

## 3.2.16 密钥备份

新增：

| 编号     | 功能     |
| -------- | -------- |
| F-KM-013 | 密钥备份 |

要求：

* 备份数据必须加密；
* 备份文件不得包含明文密钥；
* 备份必须具备完整性保护；
* 备份数据与生产环境隔离；
* 备份操作必须审计。

---

## 3.2.17 密钥恢复

新增：

| 编号     | 功能     |
| -------- | -------- |
| F-KM-014 | 密钥恢复 |

恢复必须：

* 验证备份完整性；
* 验证备份来源；
* 校验密钥版本；
* 校验目标环境；
* 记录恢复人员；
* 记录恢复时间；
* 记录恢复结果。

---

## 3.2.18 密钥恢复验证

新增：

| 编号     | 功能     |
| -------- | -------- |
| F-KM-015 | 恢复验证 |

恢复后必须通过测试确认：

* 密钥可正常识别；
* KeyId/Version 正确；
* SM2 能够完成约定密码操作；
* SM4 能够完成约定密码操作；
* HMAC 能够完成约定密码操作；
* 密钥状态符合恢复策略。

---

## 3.2.19 密钥版本管理

| 编号     | 功能         |
| -------- | ------------ |
| F-KM-011 | 密钥版本管理 |

要求：

* 每次轮换产生新版本；
* 版本号单调递增；
* 保留版本链；
* 支持查询历史版本；
* 支持按版本执行允许的历史密码操作；
* 不允许历史版本重新成为当前版本。

---

## 3.2.20 密钥隔离

必须实现：

> App A → 只能访问 App A 授权的 Key

禁止：

> App A → 通过猜测 KeyId、修改请求参数等方式访问 App B 的 Key。

后端必须执行 AppId 与 KeyId 的归属校验。

---

# 3.3 应用认证与访问控制

## 3.3.1 应用管理

| 编号       | 功能           |
| ---------- | -------------- |
| F-AUTH-001 | 应用注册       |
| F-AUTH-002 | 应用查询       |
| F-AUTH-003 | 应用启用/禁用  |
| F-AUTH-004 | AppSecret 重置 |
| F-AUTH-005 | AppSecret 轮换 |
| F-AUTH-006 | AppSecret 撤销 |

---

## 3.3.2 AppSecret 生命周期

AppSecret 应具有：

```text
CREATED → ACTIVE → ROTATED / DISABLED / REVOKED
```

要求：

* 创建时只展示一次明文；
* 平台原则上不得提供“找回原 Secret”功能；
* Secret 丢失只能重新生成；
* 新 Secret 生效后旧 Secret 按策略失效；
* Secret 重置必须审计；
* 高风险操作必须二次认证。

---

## 3.3.3 Token

| 编号       | 功能       |
| ---------- | ---------- |
| F-AUTH-010 | 获取 Token |
| F-AUTH-011 | 刷新 Token |
| F-AUTH-012 | Token 校验 |
| F-AUTH-013 | Token 撤销 |

Token 至少应包含：

* iss；
* sub；
* aud；
* iat；
* exp；
* jti；
* appid；
* scope。

Token 必须具备有效期。

Token 撤销机制必须在详细设计中明确，不得仅以“JWT 无状态”为理由忽略撤销要求。

---

## 3.3.4 权限控制

| 编号       | 功能          |
| ---------- | ------------- |
| F-AUTH-020 | API 权限分配  |
| F-AUTH-021 | KeyUsage 权限 |
| F-AUTH-022 | 配额管理      |
| F-AUTH-023 | IP 白名单     |
| F-AUTH-024 | 请求签名权限  |

应用权限至少应区分：

* KEY_READ；
* KEY_USE；
* KEY_CREATE；
* KEY_ROTATE；
* KEY_DISABLE；
* KEY_REVOKE；
* KEY_DESTROY；
* CRYPTO_SM2；
* CRYPTO_SM3；
* CRYPTO_SM4；
* CRYPTO_HMAC；
* AUDIT_READ。

---

# 3.4 审计与安全管理

## 3.4.1 操作审计

| 编号        | 功能           |
| ----------- | -------------- |
| F-AUDIT-001 | 密码操作审计   |
| F-AUDIT-002 | 密钥管理审计   |
| F-AUDIT-003 | 认证审计       |
| F-AUDIT-004 | 管理操作审计   |
| F-AUDIT-005 | 审计查询       |
| F-AUDIT-006 | 审计导出       |
| F-AUDIT-007 | 审计完整性验证 |
| F-AUDIT-008 | 配置变更审计   |

---

## 3.4.2 审计字段

每条审计记录至少包括：

| 字段         | 说明                  |
| ------------ | --------------------- |
| AuditId      | 审计唯一标识          |
| Timestamp    | 操作时间              |
| RequestId    | 请求唯一标识          |
| TraceId      | 请求追踪标识          |
| OperatorType | APP/ADMIN/USER/SYSTEM |
| OperatorId   | 操作主体              |
| OperatorName | 操作主体名称          |
| AppId        | 应用标识              |
| SourceIP     | 来源 IP               |
| Operation    | 操作类型              |
| KeyId        | 密钥标识              |
| KeyVersion   | 密钥版本              |
| Result       | 成功/失败             |
| ErrorCode    | 错误码                |
| Duration     | 耗时                  |
| PreviousHash | 上一条日志哈希        |
| CurrentHash  | 当前日志哈希          |

---

## 3.4.3 审计敏感数据

审计日志禁止记录：

* AppSecret；
* 私钥；
* SM4 明文密钥；
* HMAC 明文密钥；
* Root Key；
* Token 完整内容；
* 明文业务数据；
* 完整密码请求体。

必要情况下仅记录：

* 数据长度；
* 数据摘要；
* 参数类型；
* KeyId；
* 业务关联标识。

---

## 3.4.4 日志防篡改

审计日志必须采用追加写入模式。

要求：

1. 日志写入后不得通过普通业务接口修改；
2. 普通管理员不得删除；
3. 审计员只能查询和导出；
4. 使用 SM3 或其他项目确定的完整性机制形成哈希链；
5. 定期形成完整性校验点；
6. 可将日志归档到独立存储；
7. 支持审计完整性验证。

---

# 3.5 风险控制与安全事件

## 3.5.1 异常检测

| 编号       | 功能               |
| ---------- | ------------------ |
| F-RISK-001 | 认证失败检测       |
| F-RISK-002 | 异常调用检测       |
| F-RISK-003 | 密钥异常使用检测   |
| F-RISK-004 | 管理员异常登录检测 |
| F-RISK-005 | 大量解密检测       |
| F-RISK-006 | 大量签名检测       |
| F-RISK-007 | HSM 异常检测       |
| F-RISK-008 | 审计完整性异常检测 |

---

## 3.5.2 安全事件

安全事件至少包括：

* AppSecret 连续认证失败；
* Token 异常；
* KeyId 越权访问；
* 短时间大量解密；
* 短时间大量签名；
* 密钥频繁轮换；
* 管理员异常登录；
* 密码设备故障；
* 数据库异常；
* Redis 异常；
* 审计完整性失败。

事件生命周期：

```text
发现 → 告警 → 确认 → 处置 → 恢复 → 关闭
```

---

# 3.6 管理控制台

## 3.6.1 管理角色

V1.1 不再仅采用简单 admin/user 双角色。

建议至少支持：

| 角色       | 权限                     |
| ---------- | ------------------------ |
| 系统管理员 | 系统配置、用户及应用管理 |
| 密钥管理员 | 密钥生命周期管理         |
| 安全审计员 | 审计查询、审计验证、导出 |
| 应用管理员 | 管理授权范围内应用       |
| 运维管理员 | 运行状态、设备、监控     |

实际角色数量可根据项目组织架构进行裁剪。

关键原则：

> 密钥管理权限、系统管理权限、审计权限应具备职责分离能力。

---

## 3.6.2 用户管理

| 编号      | 功能          |
| --------- | ------------- |
| F-CON-001 | 登录          |
| F-CON-002 | 登出          |
| F-CON-003 | 修改密码      |
| F-CON-004 | 用户创建      |
| F-CON-005 | 用户查询      |
| F-CON-006 | 用户启用/禁用 |
| F-CON-007 | 重置密码      |
| F-CON-008 | 角色分配      |

---

## 3.6.3 管理员登录安全

要求：

* 密码登录；
* 验证码/风险控制；
* 连续失败锁定；
* Session/Token 设置空闲超时；
* 设置绝对会话超时；
* Cookie 设置 Secure；
* Cookie 设置 HttpOnly；
* 根据部署场景设置 SameSite；
* 登录、登出、失败均审计。

---

## 3.6.4 高风险操作

以下操作必须执行二次确认：

* 密钥销毁；
* 密钥恢复；
* AppSecret 重置；
* AppSecret 撤销；
* Root Key 相关操作；
* 权限变更；
* 管理员角色变更；
* 安全策略修改。

---

# 3.7 密码设备管理

新增模块。

| 编号      | 功能              |
| --------- | ----------------- |
| F-DEV-001 | 密码设备注册      |
| F-DEV-002 | 密码设备查询      |
| F-DEV-003 | 密码设备启用/禁用 |
| F-DEV-004 | 健康检查          |
| F-DEV-005 | 主备配置          |
| F-DEV-006 | 故障切换          |
| F-DEV-007 | 性能状态监控      |

设备信息至少包括：

* DeviceId；
* DeviceName；
* DeviceType；
* 厂商；
* 型号；
* IP/连接信息；
* 状态；
* 支持算法；
* 主备关系；
* 最后健康检查时间。

---

# 3.8 系统配置管理

平台配置至少包括：

* Token 有效期；
* AppSecret 策略；
* 登录失败次数；
* 管理员 Session 超时；
* API 限流；
* API 配额；
* 密钥默认有效期；
* 密钥轮换策略；
* 审计保留周期；
* 告警策略；
* IP 白名单；
* 密码设备策略。

所有安全配置变更必须记录审计。

---

# 3.9 健康检查与运行状态

新增：

```text
GET /health
GET /live
GET /ready
```

健康状态至少区分：

* API 服务；
* 数据库；
* Redis；
* 密码模块；
* HSM；
* 审计服务。

“服务进程正常”不能等同于“密码服务可用”。

---

# 4. 非功能需求

# 4.1 性能需求

V1.0 指标保留，但 V1.1 明确测试条件。

| 指标          | 要求                  |
| ------------- | --------------------- |
| SM4 吞吐量    | 单节点不低于 50 MB/s  |
| SM3 吞吐量    | 单节点不低于 100 MB/s |
| SM2 签名/验签 | 单节点不低于 500 次/s |
| 并发请求      | 不低于 500            |
| API P99       | 不超过 50ms，不含网络 |

性能测试必须明确：

* 数据块大小；
* 并发线程数；
* CPU；
* 内存；
* 软件密码模块/HSM；
* 是否包含数据库；
* 是否包含审计；
* 是否包含网络。

SM2、SM4、SM3 的软件密码模块性能与 HSM 性能应分别测试。

---

# 4.2 可用性需求

| 指标       | 要求     |
| ---------- | -------- |
| 系统可用性 | ≥99.95% |
| RTO        | ≤30分钟 |
| RPO        | ≤5分钟  |

高可用必须覆盖：

* 应用节点故障；
* API 节点故障；
* Redis 故障；
* 数据库故障；
* HSM 故障；
* 网络故障；
* 单机房故障。

---

# 4.3 可扩展性

支持：

* 应用节点水平扩展；
* 密码服务水平扩展；
* HSM 节点扩展；
* 数据库扩展；
* 审计存储扩展。

容量应支持至少当前业务量的 3～5 倍扩展。

---

# 4.4 可维护性

要求：

* 健康检查；
* 运行监控；
* 日志统一输出；
* 配置中心；
* 告警；
* 密码设备状态监控；
* 审计查询；
* 故障诊断。

---

# 4.5 兼容性

API：

* RESTful；
* JSON；
* UTF-8；
* HTTPS；
* Base64。

SDK 是否纳入 V1.1 交付范围，应根据项目实际建设范围确定。

若第一期以 REST API 为主，则 OpenAPI 规范优先于 SDK 开发。

---

# 4.6 安全性

必须满足：

* TLS；
* 身份认证；
* 权限控制；
* 密钥隔离；
* 防重放；
* 请求签名；
* 审计；
* 日志防篡改；
* 敏感数据脱敏；
* 数据备份加密；
* 数据库最小权限。

---

# 4.7 容量需求

应定义：

* 最大应用数量；
* 最大密钥数量；
* 单应用最大密钥数量；
* 每日 API 调用量；
* 每秒最大请求数；
* 单请求最大数据量；
* 审计日志最大增长量；
* 备份容量。

实际数值在详细设计阶段根据项目规模确定。

---

# 4.8 时间同步需求

平台所有节点必须使用统一时间源。

时间同步用于：

* Token exp；
* 密钥过期；
* 审计时间；
* 安全事件时间；
* 日志哈希链；
* 备份；
* 证书有效期。

应配置统一 NTP/时间同步服务。

---

# 5. 运行环境

## 5.1 硬件环境

V1.0 配置作为基础要求。

| 环境         | 最低配置 | 推荐配置  |
| ------------ | -------- | --------- |
| 应用服务器   | 4C/8GB   | 8C/16GB   |
| 数据库服务器 | 4C/16GB  | 8C/32GB   |
| HSM          | 支持国密 | 双机/集群 |

实际生产配置应根据性能测试结果调整。

---

# 5.2 操作系统

支持：

* 麒麟；
* 统信；
* 其他项目指定国产操作系统；
* Linux 兼容环境。

具体版本必须形成兼容性矩阵。

---

# 5.3 数据库

支持项目确定的国产数据库或 MySQL/PostgreSQL。

详细设计阶段应明确：

* 数据库版本；
* 字符集；
* 排序规则；
* 驱动版本；
* EF/JDBC 等访问组件；
* 国产数据库兼容差异。

---

# 5.4 缓存及中间件

可采用：

* Redis；
* 消息队列；
* 日志平台。

非核心组件不得成为密码服务单点故障。

---

# 5.5 密码设备

如采用 HSM，必须明确：

* 厂商；
* 型号；
* 国密算法支持情况；
* SM2 支持情况；
* SM4 支持情况；
* HMAC-SM3 支持情况；
* 随机数能力；
* 密钥生成能力；
* 密钥导入导出能力；
* HA 能力；
* 国产化适配情况。

---

# 5.6 网络环境

* API 使用 HTTPS；
* 管理接口与业务接口隔离；
* 密码设备网络独立；
* 数据库不直接暴露公网；
* 管理网络限制访问来源；
* 支持 IP 白名单。

---

# 5.7 国产化适配要求

形成以下兼容矩阵：

| 类型       | 需要确认               |
| ---------- | ---------------------- |
| CPU        | x86 / ARM64            |
| OS         | 麒麟/统信等            |
| DB         | 国产数据库版本         |
| JDK/.NET   | 实际运行时版本         |
| Redis      | 版本                   |
| HSM        | 厂商/型号              |
| 浏览器     | Chrome/Edge/国产浏览器 |
| Web Server | Nginx/Apache/其他      |
| 密码库     | 实际密码组件           |

---

# 6. 接口规范

# 6.1 通用约定

* HTTPS；
* JSON；
* UTF-8；
* Base64；
* ISO 8601；
* Authorization Bearer Token。

所有 API 必须返回 RequestId。

---

# 6.2 认证要求

```http
Authorization: Bearer {access_token}
```

高安全接口可以要求：

```text
Authorization
X-App-Id
X-Timestamp
X-Nonce
X-Signature
X-Request-Id
```

---

# 6.3 请求与响应格式

成功：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "requestId": "req-xxxx",
  "timestamp": "2026-09-17T10:30:00.000+08:00"
}
```

错误：

```json
{
  "code": 30001,
  "message": "SM4 operation failed",
  "data": null,
  "requestId": "req-xxxx",
  "timestamp": "2026-09-17T10:30:00.000+08:00"
}
```

不得向外部返回：

* 堆栈；
* 内部路径；
* SQL；
* 密码库异常；
* Padding 内部细节；
* HSM 内部错误细节；
* 密钥材料。

---

# 6.4 密码服务接口

| 接口      | 方法 | 路径                             |
| --------- | ---- | -------------------------------- |
| SM2 加密  | POST | `/api/v1/crypto/sm2/encrypt`   |
| SM2 解密  | POST | `/api/v1/crypto/sm2/decrypt`   |
| SM2 签名  | POST | `/api/v1/crypto/sm2/sign`      |
| SM2 验签  | POST | `/api/v1/crypto/sm2/verify`    |
| SM3 Hash  | POST | `/api/v1/crypto/sm3/hash`      |
| SM4 加密  | POST | `/api/v1/crypto/sm4/encrypt`   |
| SM4 解密  | POST | `/api/v1/crypto/sm4/decrypt`   |
| HMAC 生成 | POST | `/api/v1/crypto/hmac/generate` |
| HMAC 验证 | POST | `/api/v1/crypto/hmac/verify`   |
| 随机数    | POST | `/api/v1/crypto/random`        |
| 算法能力  | GET  | `/api/v1/crypto/algorithms`    |

随机数由 GET 调整为 POST，避免未来扩展参数进入 URL、缓存或日志。

---

# 6.5 密钥管理接口

| 接口     | 方法 | 路径                                     |
| -------- | ---- | ---------------------------------------- |
| 创建密钥 | POST | `/api/v1/keys`                         |
| 查询密钥 | GET  | `/api/v1/keys/{keyId}`                 |
| 密钥列表 | GET  | `/api/v1/keys`                         |
| 激活     | POST | `/api/v1/keys/{keyId}/activate`        |
| 禁用     | POST | `/api/v1/keys/{keyId}/disable`         |
| 重新激活 | POST | `/api/v1/keys/{keyId}/enable`          |
| 轮换     | POST | `/api/v1/keys/{keyId}/rotate`          |
| 注销     | POST | `/api/v1/keys/{keyId}/revoke`          |
| 销毁     | POST | `/api/v1/keys/{keyId}/destroy`         |
| 导入     | POST | `/api/v1/keys/import`                  |
| 备份     | POST | `/api/v1/keys/{keyId}/backup`          |
| 恢复     | POST | `/api/v1/keys/restore`                 |
| 恢复验证 | POST | `/api/v1/keys/{keyId}/verify-recovery` |
| 版本查询 | GET  | `/api/v1/keys/{keyId}/versions`        |

---

# 6.6 应用认证接口

| 接口    | 方法 | 路径                           |
| ------- | ---- | ------------------------------ |
| Token   | POST | `/api/v1/auth/token`         |
| Refresh | POST | `/api/v1/auth/token/refresh` |
| Revoke  | POST | `/api/v1/auth/token/revoke`  |

---

# 6.7 审计接口

| 接口       | 方法 | 路径                     |
| ---------- | ---- | ------------------------ |
| 审计查询   | GET  | `/api/v1/audit/logs`   |
| 审计导出   | POST | `/api/v1/audit/export` |
| 完整性验证 | POST | `/api/v1/audit/verify` |

---

# 6.8 系统及健康检查接口

```text
GET /health
GET /live
GET /ready
GET /api/v1/system/info
GET /api/v1/crypto/algorithms
```

---

# 6.9 幂等与防重放

以下操作必须支持幂等控制：

* 密钥轮换；
* 密钥销毁；
* 密钥注销；
* AppSecret 重置；
* Token 撤销；
* 密钥恢复。

建议支持：

```http
Idempotency-Key: xxx
```

涉及敏感操作的 API 必须支持：

* Timestamp；
* Nonce；
* 请求签名。

时间窗口由平台配置。

---

# 6.10 错误码

| 范围        | 类别      |
| ----------- | --------- |
| 10000-10099 | 认证      |
| 11000-11099 | Token     |
| 20000-20099 | 参数      |
| 30000-30099 | 密码服务  |
| 40000-40099 | 密钥管理  |
| 50000-50099 | 系统      |
| 60000-60099 | 限流/配额 |
| 70000-70099 | 密码设备  |
| 80000-80099 | 安全事件  |

---

# 6.11 接口版本管理

API 使用：

```text
/api/v1/
```

后续重大不兼容变更使用：

```text
/api/v2/
```

V1 接口废弃必须提供：

* 废弃通知；
* 迁移说明；
* 兼容周期；
* 新接口说明。

---

# 7. 数据需求

# 7.1 核心数据实体

至少包括：

1. Application；
2. ApplicationSecret；
3. User；
4. Role；
5. Permission；
6. Key；
7. KeyVersion；
8. Token；
9. AuditLog；
10. SecurityEvent；
11. AlertRule；
12. CryptoDevice；
13. SystemConfig；
14. BackupRecord。

---

# 7.2 密钥数据

Key 数据至少包括：

```text
KeyId
KeyType
KeyUsage
AppId
CurrentVersion
Status
CreatedAt
ActivatedAt
ExpireAt
RotatedAt
RevokedAt
DestroyedAt
ProviderId
AlgorithmParams
MetadataHash
```

密钥材料独立于普通元数据管理。

---

# 7.3 应用及凭据数据

Application：

* AppId；
* Name；
* Status；
* IP 白名单；
* API 权限；
* 配额；
* 创建时间；
* 更新时间。

ApplicationSecret：

* SecretId；
* AppId；
* SecretHash；
* Status；
* CreatedAt；
* ExpireAt；
* RevokedAt。

AppSecret 原文不得存储。

---

# 7.4 审计数据

审计日志独立于普通业务数据。

至少记录：

* 操作主体；
* AppId；
* KeyId；
* 操作；
* 时间；
* IP；
* RequestId；
* TraceId；
* 结果；
* 错误码；
* 耗时。

---

# 7.5 安全事件数据

SecurityEvent：

* EventId；
* EventType；
* Severity；
* Source；
* AppId；
* KeyId；
* FirstDetectedAt；
* LastDetectedAt；
* Status；
* Handler；
* HandleTime；
* HandleResult。

---

# 7.6 数据完整性

以下数据必须具备完整性保护：

* Key 元数据；
* Application；
* Permission；
* SecurityConfig；
* AuditLog；
* BackupRecord。

---

# 7.7 数据保留

V1.0 的基本策略保留：

| 数据           | 保留             |
| -------------- | ---------------- |
| 审计日志       | ≥3年            |
| 销毁密钥元数据 | 长期保留         |
| Token记录      | 过期后按策略清理 |
| 应用信息       | 注销后按策略保留 |

最终保留期限应以项目实际安全管理制度和验收要求为准。

---

# 7.8 数据备份

要求：

* 生产数据定期备份；
* 备份数据加密；
* 生产与备份环境隔离；
* 密钥相关数据必须重点保护；
* 审计日志独立归档；
* 备份结果必须记录；
* 备份失败必须告警。

---

# 7.9 数据恢复验证

仅“备份成功”不能作为恢复能力的验收依据。

必须定期进行：

```text
备份
 ↓
恢复
 ↓
完整性验证
 ↓
密码操作验证
 ↓
数据一致性验证
 ↓
恢复结果记录
```

---

# 7.10 数据库权限

生产应用不得使用数据库超级账号。

至少区分：

* 应用账号；
* 数据库迁移账号；
* 运维账号；
* 审计只读账号。

数据库账号遵循最小权限原则。

---

# 8. 安全需求

# 8.1 密钥安全

必须：

* 禁止明文存储；
* 禁止日志输出；
* 禁止 API 返回；
* 禁止普通业务系统直接访问；
* 使用 Root Key/KEK/HSM 保护；
* 密钥材料安全擦除；
* 密钥导入加密；
* 备份加密。

生产环境 Root Key 的实际保护方式必须在详细设计阶段根据实际密码设备确定。

---

# 8.2 传输安全

* TLS 1.2 及以上；
* 根据实际部署支持国密 TLS；
* 管理接口与业务接口隔离；
* 密码设备通信采用安全连接；
* 禁止明文传输密码材料。

---

# 8.3 身份认证安全

App：

* AppID；
* AppSecret；
* Token；
* 可选请求签名。

管理人员：

* 用户名；
* 密码；
* 验证码；

---

# 8.4 权限控制

采用 RBAC + 资源归属控制。

权限检查顺序：

```text
身份认证
 ↓
角色检查
 ↓
API权限
 ↓
AppId权限
 ↓
KeyId归属
 ↓
KeyUsage
 ↓
Key状态
```

---

# 8.5 管理员安全

管理员必须支持：

* 密码策略；
* 登录失败锁定；
* Session 超时；
* 高风险操作二次认证；
* 全量管理审计。

---

# 8.6 Token 安全

Token 必须：

* 签名；
* 设置 exp；
* 设置 aud；
* 设置 iss；
* 设置 jti；
* 防止重放；
* 支持撤销；
* 不得写入普通业务日志。

---

# 8.7 API 安全

API 必须支持：

* 参数校验；
* Body 大小限制；
* 字段长度限制；
* 限流；
* 配额；
* 防重放；
* 请求签名；
* 幂等；
* CORS 控制；
* Security Headers。

---

# 8.8 审计安全

审计日志：

* 追加写入；
* 不允许普通业务接口修改；
* 不允许普通管理员删除；
* 支持完整性验证；
* 支持独立归档；
* 支持定期备份。

---

# 8.9 日志安全

日志禁止包含：

* AppSecret；
* PrivateKey；
* SM4 Key；
* HMAC Key；
* Token；
* 明文业务数据。

必要时使用：

* 脱敏；
* 摘要；
* 长度；
* KeyId。

---

# 8.10 密码设备安全

密码设备：

* 独立网络；
* 访问认证；
* 主备；
* 健康检查；
* 故障告警；
* 权限隔离；
* 操作审计。

---

# 8.11 安全事件处置

重大安全事件至少包括：

* Root Key 风险；
* HSM 故障；
* 大量密钥操作；
* AppSecret 泄露风险；
* 审计完整性异常；
* 未授权访问；
* 大量解密；
* 大量签名。

安全事件必须形成：

```text
发现 → 告警 → 研判 → 处置 → 恢复 → 关闭 → 审计
```

---

# 9. 需求修正与补充说明

# 9.1 V1.0 逻辑修正

| 编号 | V1.0 问题                               | V1.1 修正                                    |
| ---- | --------------------------------------- | -------------------------------------------- |
| C-01 | “使用”作为生命周期状态                | 调整为 ACTIVE 状态下的行为                   |
| C-02 | AppSecret 与密码学密钥混淆              | 独立作为应用凭据管理                         |
| C-03 | SM4 模式约束不足                        | 明确 ECB/CBC/CTR/GCM 参数要求                |
| C-04 | SM2 编码写死                            | 改为实际密码库/设备能力 + 互操作要求         |
| C-05 | 审计主体缺失                            | 增加 OperatorType/Id/Name                    |
| C-06 | HSM 可选与 Root Key 必须 HSM 表述不一致 | 明确生产部署模式需根据安全要求和实际设备确定 |
| C-07 | JWT 撤销机制不明确                      | 增加 Token 生命周期及撤销要求                |
| C-08 | 随机数采用 GET                          | 调整为 POST                                  |
| C-09 | 日志防篡改使用“建议”                  | 改为必须满足完整性保护                       |
| C-10 | admin/user 角色过于简单                 | 增加职责分离能力                             |

---

# 9.2 V1.1 新增需求

| 编号 | 新增内容             |
| ---- | -------------------- |
| N-01 | 密钥导入             |
| N-02 | 密钥备份             |
| N-03 | 密钥恢复             |
| N-04 | 密钥恢复验证         |
| N-05 | 密钥轮换策略         |
| N-06 | 生命周期状态转换矩阵 |
| N-07 | AppSecret 生命周期   |
| N-09 | Token 完整生命周期   |
| N-10 | RequestId/TraceId    |
| N-11 | 幂等机制             |
| N-12 | 防重放机制           |
| N-13 | 密码设备管理         |
| N-14 | 安全事件管理         |
| N-15 | 健康检查             |
| N-16 | NTP 时间同步         |
| N-17 | 数据恢复验证         |
| N-18 | 数据库最小权限       |
| N-19 | 敏感数据日志控制     |
| N-20 | API 算法能力查询     |
| N-21 | API 版本兼容策略     |
| N-22 | 性能测试条件         |
| N-23 | 国产化兼容性矩阵     |
| N-24 | 需求与测试追踪矩阵   |

---

# 9.3 V1.1 明确事项

以下内容必须在详细设计阶段最终确认：

1. HSM 厂商及型号；
2. HSM 与软件密码模块的生产使用策略；
3. Root Key 的具体保护方式；
4. SM2 密文编码；
5. SM2 签名编码；
6. SM4-GCM 参数；
7. SM4-CBC 参数；
8. Token 撤销实现方式；
9. 请求签名算法及签名规范；
10. 国产数据库型号及版本；
11. 国产操作系统及 CPU 架构；
12. 性能测试硬件环境；
13. 审计日志存储方式；
14. 备份存储方式；
15. HSM 主备/集群方案。

---

# 9.4 V1.1 范围边界

以下能力可以作为后续扩展：

* 完整 SDK；
* 多租户；
* 跨机房灾备；
* 高级行为分析；
* 密钥自动化策略编排；
* 外部 SIEM 深度集成；
* 双人审批工作流；
* 更复杂的密码设备集群调度。

如果项目合同或安全等级要求这些能力，则应在项目范围内单独列项。

---

# 10. 测试与验收需求

# 10.1 需求追踪

每项功能必须具备：

```text
需求编号
 ↓
设计模块
 ↓
接口/数据库
 ↓
代码实现
 ↓
测试用例
 ↓
验收结果
```

---

# 10.2 功能测试

至少覆盖：

* 密钥生成；
* 密钥查询；
* 激活；
* 禁用；
* 重新激活；
* 轮换；
* 过期；
* 注销；
* 销毁；
* 导入；
* 备份；
* 恢复；
* 恢复验证；
* SM2；
* SM3；
* SM4；
* HMAC；
* Token；
* AppSecret；
* 审计；
* 告警。

---

# 10.3 算法测试

SM2：

* 加密；
* 解密；
* 签名；
* 验签；
* 编码兼容。

SM3：

* 标准测试向量；
* 大数据；
* 流式。

SM4：

* 各支持模式；
* 加密/解密；
* Padding；
* GCM Tag；
* AAD；
* Nonce。

HMAC-SM3：

* 标准测试向量；
* 正确值；
* 错误值；
* 边界数据。

---

# 10.4 密钥生命周期测试

必须验证所有状态转换：

```text
CREATED
ACTIVE
ROTATED
DISABLED
EXPIRED
REVOKED
DESTROYED
```

重点验证：

* 非法状态转换；
* 越权操作；
* 已销毁 KeyId；
* 已过期 KeyId；
* 已撤销 KeyId；
* 历史版本。

---

# 10.5 安全测试

至少包括：

* 身份认证测试；
* 越权测试；
* App 隔离测试；
* KeyId 越权测试；
* Token 篡改测试；
* Token 重放测试；
* Nonce 重放测试；
* 请求签名测试；
* SQL 注入；
* XSS；
* CSRF；
* CORS；
* 敏感信息泄露；
* 日志泄露；
* 暴力破解；
* API 限流。

---

# 10.6 性能测试

必须记录：

* 测试服务器配置；
* CPU；
* 内存；
* 数据大小；
* 并发量；
* QPS；
* P50；
* P95；
* P99；
* 错误率；
* HSM/软件密码模块。

---

# 10.7 高可用测试

测试：

* 应用节点宕机；
* API 节点宕机；
* Redis 宕机；
* 数据库主节点故障；
* HSM 主设备故障；
* 网络断开；
* 密码设备恢复；
* 节点恢复。

---

# 10.8 备份恢复测试

必须至少验证：

1. 备份成功；
2. 备份完整性；
3. 备份解密；
4. 数据恢复；
5. 密钥恢复；
6. 密钥密码运算；
7. 审计数据恢复；
8. 数据一致性。

---

# 10.9 国产化兼容性测试

至少测试：

* 国产 CPU；
* 国产操作系统；
* 国产数据库；
* 国产密码设备；
* 国产浏览器；
* 国产密码库。

---

# 11. 需求编号与追踪矩阵

---

*文档结束*
